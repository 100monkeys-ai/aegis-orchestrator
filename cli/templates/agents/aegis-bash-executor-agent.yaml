apiVersion: 100monkeys.ai/v1
kind: Agent
metadata:
  name: aegis-bash-executor-agent
  version: "1.0.0"
  description: Writes a bash script that fulfills a given intent and saves it to a workspace file. Used by the intent-to-execution pipeline as the WRITE_CODE step.
  labels:
    role: worker
    category: platform
    builtin: "true"
spec:
  security_context: aegis-system-agent-runtime
  runtime:
    language: bash
    version: "5"
    model: smart
    isolation: inherit
    temperature: 0.2

  task:
    instruction: |
      You write a bash script and save it to /workspace/solution.sh using fs.write.

      Rules:
      1. Standard POSIX utilities only — no third-party packages. Use Python one-liners for JSON parsing if jq is unavailable.
      2. Define a `solve()` function containing the main logic.
      3. Read inputs from `$INTENT_INPUTS`. The variable contains a JSON object — use its top-level keys directly.
      4. Call the function at the end of the script and echo the result.
      5. The `INTENT_INPUTS` JSON uses the EXACT keys shown in the prompt below. Use ONLY those key names — do NOT guess, rename, or wrap them.
      6. Print the result to stdout via `echo`.
    prompt_template: |
      {{instruction}}

      {{intent}}

      {{input}}

  execution:
    mode: iterative
    max_iterations: 3
    llm_timeout_seconds: 120

  tools:
    - fs.write

  security:
    network:
      mode: none
    filesystem:
      read: []
      write:
        - /workspace
    resources:
      cpu: 500
      memory: 256Mi
      timeout: 300s
