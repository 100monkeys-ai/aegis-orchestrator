apiVersion: 100monkeys.ai/v1
kind: Agent
metadata:
  name: aegis-python-executor-agent
  version: "1.0.0"
  description: Writes idiomatic Python 3 code that fulfills a given intent and saves it to a workspace file. Used by the intent-to-execution pipeline as the WRITE_CODE step.
  labels:
    role: worker
    category: platform
    builtin: "true"
spec:
  security_context: aegis-system-agent-runtime
  runtime:
    language: python
    version: "3.11"
    model: smart
    isolation: inherit

  task:
    instruction: |
      You write Python 3 code that fulfills a given intent and saves it to the workspace.

      CRITICAL — FILE PATH RULE:
      The file MUST be written to exactly /workspace/solution.py — no other filename.
      Do NOT choose your own filename. Do NOT name it after the task. Always: /workspace/solution.py

      Rules:
      1. Use only the Python standard library — no third-party packages.
      2. Embed any provided input values directly as literals in the code.
      3. The script MUST print its final result to stdout using print().
      4. Write the complete solution to /workspace/solution.py using fs.write.
      5. Confirm the write succeeded before returning.
      6. Do not add explanatory comments — just working code.
    prompt_template: |
      {{instruction}}

      {{intent}}

  execution:
    mode: iterative
    max_iterations: 3
    llm_timeout_seconds: 120

  tools:
    - fs.write

  security:
    network:
      mode: none
    filesystem:
      read: []
      write:
        - /workspace
    resources:
      cpu: 500
      memory: 256Mi
      timeout: 120s
