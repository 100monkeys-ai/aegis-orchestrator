version: "1.1"
agent:
  name: "echo"
  runtime: "python:3.11"
task:
  instruction: "Echo the input back to the user."
  prompt_template: |
    {agent_instruction}
    
    User: {user_input}
    Daemon:
execution:
  mode: "iterative"
  max_retries: 3
